# wellmanifest/policy-dsl

Governed standard and reference conformance implementation for the declarative
Policy DSL used by `wellmanifest/new-project` contributor policy documents.

The language describes rules, conditions, obligations, prohibitions,
assertions and state transitions. It is inert policy data: parsing or deciding
never executes a shell command or grants effect authority.

Implementation is tracked by `project/ticket-001`.
