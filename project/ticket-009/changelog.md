# Changelog — ticket-009

- Added `examples/sales/fixtures/www-plans.facade.json` and
  `profiles/sales/www-plans.lock.json` for durable `compare-www-plans` CI.
- Updated sales profile tests/docs and `dsl-manifest.json` digests (LC-031).
